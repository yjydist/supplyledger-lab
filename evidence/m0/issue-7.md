# M0 issue #7 — stage exit verification

- Checked at: 2026-09-24T11:36:21Z.
- Issue: https://github.com/yjydist/supplyledger-lab/issues/7
- Test IDs: M0-EXIT-00 through M0-EXIT-05; later network checks are **NOT RUN**.
- Specification: §§2.1–2.3, 5.1, 19, 19.1, 20.6; M0 exit conditions. This verifies the M0 toolchain and static bootstrap configuration, not the §3.5 formal network.
- Source commit: the commit introducing this report; after commit, resolve its full SHA with `git log --diff-filter=A -1 --format=%H -- evidence/m0/issue-7.md`. Pre-change baseline: `7fb86e099ebb22401c6ecb9714d6e86d5f738b23` (issue #6). The version lock's `sourceCommits.repositoryBaseline` records the earlier image/tool measurement baseline, not this report's source commit.
- Version lock: SHA-256 `422fba14294aaf9f0c40862fcd112ed3d2ed664cd5294c7bfe0aa14477fa234b` for `versions.lock.yaml`; host `darwin/arm64`, native Linux containers `linux/arm64`. This report uses the corrected lock; earlier issue reports retain the digests of the locks they actually tested.
- Compose tier: `bootstrap` only. `compose/bootstrap.yaml` parsed without starting a Compose service; the formal network Compose, `dev`, and `resilience` tiers are **NOT RUN**.
- Network identity and genesisHash: **NOT RUN**; no CA or channel exists. txId, block height, validation code, and commit status: **NOT RUN**; no transaction was submitted.
- Prepared data: previously built local `supply-tools:m0-fabric3.1.5-ca1.5.22`, the seven pinned official image references in the lock, and the two module `go.mod`/`go.sum` files. The verifier used disposable containers and a temporary in-container Go module/build cache; doctor created and removed a temporary bind-mount probe. No CA material, ledger, business fixture, persistent test volume, or backup was created.
- Redaction: output below contains versions, hashes, resource counts, module names, and nonsecret image tags only. No credential, private key, token, private trade term, or local home path is included.

## Commands and observed results

```sh
bash -n scripts/verify-m0.sh
shellcheck scripts/verify-m0.sh
awk -f scripts/validate-version-lock.awk versions.lock.yaml
git add -N scripts/verify-m0.sh scripts/validate-version-lock.awk evidence/m0/issue-7.md
make verify-m0
bash -c 'go() { printf "%s\n" "go version go0.0.0 fake/arch"; }; export -f go; bash scripts/verify-m0.sh'
shasum -a 256 versions.lock.yaml
git diff --check
```

`make verify-m0` exited 0. Its first step validates the current two-space YAML mapping/scalar-list format, including syntax, unique full key paths, and quoted decimal-like values, before any lock lookup. It then runs `bash scripts/doctor.sh`, `docker compose -p supplyledger -f compose/bootstrap.yaml --profile bootstrap config --quiet`, read-only Docker image inspections, offline tools-container CLI and checksum probes, and a pinned Go builder container. The Go builder runs `go build -mod=readonly ./...`, `go test -mod=readonly ./...`, `go vet -mod=readonly ./...`, and `go mod verify` separately in `chaincode` and `app`, with `GOTOOLCHAIN=local`. The exact Docker options and lock-field resolution are in `scripts/verify-m0.sh`; the repository bind mount is read-only, and Go caches are temporary.

The version-lock validator accepted the current file. Seven temporary malformed fixtures were rejected with exit 1 before any Docker work: duplicate `platform.host`, odd indentation, missing mapping colon, flow mapping, unquoted decimal-like `17.10`, mapping key without children, and a mapping in a scalar list. The decimal fixture was generated by replacing `observedCliVersion: "17.10"` with `observedCliVersion: 17.10` in a temporary copy, then running `awk -f scripts/validate-version-lock.awk` on that copy; its diagnostic was `quote decimal-like scalar to preserve version text`. A recursive Ruby Psych parse of the corrected lock found `images.postgres.observedCliVersion` as the exact string `17.10`. Its only non-string leaves were the intended `schemaVersion` integer, `measuredAtUtc` time, and `platform.emulated` boolean. Ruby was used only for this independent audit, not by the verifier.

The verifier reported host `darwin/arm64` and Docker `linux/arm64` without emulation. Host Go `go1.27.1 darwin/arm64` was recorded as optional information; the build gate was the container's `go1.26.8 linux/arm64` with `GOTOOLCHAIN=local`. The separate exported-function probe above made host `go version` report `go0.0.0 fake/arch`; the full verifier still exited 0 and the pinned container build passed. This probe checked only host-tool independence, not Go compilation with a fake toolchain. Docker client/server were `29.8.0/29.8.0`, Compose `v5.5.1`, and Buildx `v0.37.1`, matching the measured lock. Doctor reported 14 Docker CPUs, 16,481,579,008 Docker memory bytes, 51,539,607,552 host memory bytes, and 496,400,560 workspace-free KiB. The temporary bind probe passed read/write, executable, process UID:GID `501:20`, file UID `501`, and UTC checks. Docker Desktop presented the probe file as GID `0`; doctor recorded that observation while the functional probe passed.

All seven official image index digests and `linux/arm64` platform digests matched `versions.lock.yaml`. The self-built tools image's local index digest was `sha256:6b466c4dbd8aee240cbbc4c95860c4bf0b7a0de83b3edc808bc3dc71b895f8fe`; its local platform digest was `sha256:518f2c0569e28415082bce18a7d680a2232d811085542f13d51c0ca789e60c23`. These are local Docker descriptors, not a published registry manifest; the published digest remains **NOT RUN** in the lock. The image's six required Fabric/CA CLI binary SHA-256 checks returned `OK`; version probes showed Fabric `v3.1.5`, Fabric CA `v1.5.22`, and `linux/arm64`. Diagnostic commands and the locked OS package-manifest SHA-256 matched. The apt package versions were measured and recorded in issue #4; no apt snapshot or byte-identical future rebuild was claimed.

The four module-lock file checksum checks returned `OK`. Both Go modules completed build, test command, vet, and module verification. `go test` reported `[no test files]` for `chaincode/internal/contract` and `app/internal/gateway`; these commands verify compilation and current dependency integrity, not chaincode business behavior. `go mod verify` reported `all modules verified` for each module. The executable-source scan found no sample-network or prebuilt-tools dependency and no submodule. The tracked-file path and key-marker scan found no secret/runtime artifact; `docker/tools/.dockerignore` remained an allowlist of the Dockerfile and itself. The final local scan included the new script and report through `git add -N`; rerun `make verify-m0` after commit against the committed tree.

| Test ID | Actual command or entry | Expected | Actual | Judgment |
| --- | --- | --- | --- | --- |
| M0-EXIT-00 version-lock parsing | `awk -f scripts/validate-version-lock.awk versions.lock.yaml`; malformed temporary fixtures | Current lock parses; duplicate keys, unsupported syntax and unquoted decimal-like versions fail | Current lock accepted, seven fixtures rejected; Psych read PostgreSQL version as exact string | PASS |
| M0-EXIT-01 host and version lock | `make verify-m0`; mismatched host-Go probe | Native host/container platforms and measured Docker versions match lock; host Go is informational | Host, Docker, Compose, Buildx, doctor and platform checks passed; fake host Go did not affect pinned builder | PASS |
| M0-EXIT-02 tools image and CLI | `make verify-m0` | Pinned image descriptors, local tools descriptors, CLI versions and hashes agree with lock | Seven official image pairs, local tools pair, six CLI checks and OS package manifest passed | PASS |
| M0-EXIT-03 Go modules | `make verify-m0` | Both modules build/test/vet/verify with pinned Go 1.26.8 and local toolchain | Both completed; `go test` found no test files | PASS |
| M0-EXIT-04 bootstrap Compose | `make verify-m0` | Bootstrap config parses and names locked tools image | `config --quiet` passed; one bootstrap tools service, no service started | PASS |
| M0-EXIT-05 source and context review | `make verify-m0` | No sample dependency, tracked secret artifact, or widened tools build context | Scans passed with new files visible through intent-to-add; post-commit rerun remains | PASS |
| M0-NET-01 formal network, CA, channel, genesis and transactions | No command executed | Native Fabric evidence in M1–M3 | Network and chaincode are not implemented | NOT RUN |
| M0-PUB-01 published tools manifest | No registry push or manifest inspection executed | Registry digest after publication | Only local Docker descriptors exist | NOT RUN |
| M0-TEST-01 Go business behavior tests | No test cases exist in either module | Behavioral assertions in later implementation | `go test` reported `[no test files]` | NOT RUN |

`bash -n`, ShellCheck and `git diff --check` exited 0. No M0 exit check failed. Remaining work belongs to the stated later stages; this report makes no claim about real MSP, endorsement, TLS, ledger, privacy, transaction validity, or recovery behavior.
